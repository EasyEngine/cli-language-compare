

## How To use
```base
root@example.com:~# python ee.py --help
usage: ee.py (sub-commands ...) [options ...] {arguments ...}

EasyEngine Does Amazing Things

commands:

  site
    Site commands are work under this controller

  stack
    Stack commands are listed under this controller

optional arguments:
  -h, --help  show this help message and exit
  --debug     toggle debug output
  --quiet     suppress all output

```